name: Progress email renderer

on:
  pull_request:
    paths:
      - 'apps/web/src/lib/progress-email-contract.js'
      - 'apps/web/src/lib/progress-email-email.js'
      - 'apps/web/src/lib/marketing-email-preference-token.js'
      - 'apps/web/scripts/test-progress-email-contract.mjs'
      - 'apps/web/scripts/test-progress-email-email.mjs'
      - '.github/workflows/progress-email-renderer.yml'
  push:
    branches: [main]
    paths:
      - 'apps/web/src/lib/progress-email-contract.js'
      - 'apps/web/src/lib/progress-email-email.js'
      - 'apps/web/src/lib/marketing-email-preference-token.js'
      - 'apps/web/scripts/test-progress-email-contract.mjs'
      - 'apps/web/scripts/test-progress-email-email.mjs'
      - '.github/workflows/progress-email-renderer.yml'

permissions:
  contents: read

jobs:
  progress-email-renderer:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: apps/web
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.x
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund --strict-allow-scripts

      - name: Syntax check
        run: node --check src/lib/progress-email-email.js

      - name: Existing progress payload contract
        run: node scripts/test-progress-email-contract.mjs

      - name: Progress email renderer contract
        run: node scripts/test-progress-email-email.mjs
