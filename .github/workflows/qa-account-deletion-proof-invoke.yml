# QA diagnostic trigger only. Never merge this workflow to main.
name: QA account deletion completion intent diagnostic

on:
  push:
    branches:
      - qa/account-deletion-proof-invoke-20260821

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Run pure Development completion-intent diagnostic
        shell: bash
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error \
            'https://ycstrcvshdluovtuasjc.supabase.co/functions/v1/qa-account-deletion-intent-diagnostic-20260821' \
            | jq .
