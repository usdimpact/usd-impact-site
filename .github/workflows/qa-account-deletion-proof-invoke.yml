# Retired QA branch. Never merge this workflow to main.
name: QA account deletion proof retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retired state
        run: echo 'Account deletion QA proof is retired; no destructive action is configured.'
