# Release Control Marker

This file intentionally lives inside the Vercel application root so a reviewed release-control merge can trigger the normal Git-to-Vercel Production deployment path when repository-level documentation changes are skipped by path-based build detection.

It contains no runtime configuration, secrets, customer data, commerce activation, Supabase mutation, scheduler activation, or executable application logic.

Current release-control catch-up initiated on 2026-08-22 after the prior Vercel daily deployment quota cleared.
