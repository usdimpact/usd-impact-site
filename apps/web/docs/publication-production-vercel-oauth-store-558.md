# Production Vercel OAuth refresh-credential store - issue 558

## Status

The reviewed OAuth refresh-credential store migration is installed in Production Supabase project `edkdqncrreouzmxqeypm` and remains deliberately dormant. The first owner-approved Production application attempt on 2026-09-15 failed inside its transaction at an ownership-requiring `REVOKE EXECUTE` against `publication_guard_api.read_current_revision()` and rolled back completely. The source was repaired without broadening ownership or privileges, then the renewed exact-head application succeeded.

Post-apply read-only verification showed the private schema, roles, forced-RLS table and exact column grants are present; the store contains zero rows; `fx558_oauth_store_login` remains `PASSWORD NULL`; publication revision remains `0`; release authorizations and admissions remain `0`; and neither OAuth-store role has publication-function or Vault authority. Supabase Security Advisor remained clean.

Supabase recorded the successful managed application as remote migration version `20260915164854`. The repository has been reconciled to that applied version without editing the remote migration ledger and without changing executable migration SQL.

Nothing in this source reconciliation creates or changes a live database password, credential row, AES key, Vercel App credential, OAuth token, Production environment variable, route, deployment, publication authority, admission, receipt, promotion, or merge.

The purpose of this module is to satisfy the versioned `loadRefreshCredential()` and compare-and-swap `replaceRefreshCredential()` callbacks required by `publication-production-vercel-oauth-bearer.js` without giving the runtime any publication-guard authority or generic secret-store authority.

## Repository migration provenance

The reviewed SQL contract remains `apps/web/docs/sql/publication-production-vercel-oauth-store-558.sql`.

The canonical repository migration is:

`supabase/migrations/20260915164854_publication_production_vercel_oauth_store_558.sql`

The migration was originally generated by Supabase CLI 2.117.0 as `20260915160900_publication_production_vercel_oauth_store_558.sql` in GitHub Actions run `34993123353`, job `104462528476`, using exactly:

`supabase migration new publication_production_vercel_oauth_store_558`

The successful managed Production application recorded version `20260915164854`. Repository reconciliation therefore renamed the migration to the already-recorded remote version and updated the migration-contract regression to the canonical path. The executable migration body remains identical to the reviewed SQL contract apart from comments.

The temporary filename-rehearsal workflow used only `contents: read`, no checkout, no Supabase project connection and no provider secret. It is absent from the final source state.

The live migration step is complete. Credential provisioning, OAuth App installation, encrypted-row seeding, provider rehearsal and Production activation remain separate protected actions.

## Storage choice

The reviewed store uses the existing Production guard Supabase/Postgres project `edkdqncrreouzmxqeypm`, but with a new dedicated runtime identity and a private table that contains only encrypted material.

Supabase Vault was inspected as an alternative. The live project has Vault installed, but current Supabase documentation marks Vault as public alpha. Using Vault would also require a privileged path capable of reading `vault.decrypted_secrets`, which is broader decrypted-secret authority than this runtime needs. This design therefore does not grant or depend on Vault access.

The chosen primitives are:

- GA PostgreSQL for durable row storage, row-level locking and compare-and-swap updates;
- Node `node:crypto` AES-256-GCM for encryption/decryption in trusted server code;
- a 32-byte encryption key stored only as a future Vercel Production secret;
- a dedicated Postgres login `fx558_oauth_store_login` with no publication-guard or Vault privileges; and
- a private singleton table `publication_provider_credential.vercel_oauth_refresh_state`.

The database never stores the plaintext refresh token, OAuth client secret, or AES key.

## Runtime environment contract

The dormant adapter requires these future Production-only inputs:

- `PUBLICATION_GUARD_VERCEL_OAUTH_STORE_DATABASE_URL` - Shared Supavisor transaction pooler URL for `fx558_oauth_store_login.edkdqncrreouzmxqeypm`, port 6543, database `postgres`, `sslmode=require`;
- `PUBLICATION_GUARD_PRODUCTION_READER_DATABASE_CA_CERT` - the already reviewed Supabase Production CA bundle; and
- `PUBLICATION_GUARD_VERCEL_OAUTH_STORE_KEY` - exactly 32 random bytes encoded as 64 lowercase hex characters.

The adapter only initializes in the exact Vercel Production project, GitHub repository and `main` branch context already pinned by the #558 authority design.

## Ciphertext record

The private singleton row contains only:

- `version bigint` - monotonically increasing CAS version;
- `key_fingerprint text` - SHA-256 of the 32-byte AES key, not the key itself;
- `nonce bytea` - 12-byte GCM nonce;
- `ciphertext bytea` - encrypted refresh token;
- `auth_tag bytea` - 16-byte GCM authentication tag; and
- `updated_at timestamptz`.

AES-GCM associated data binds every ciphertext to:

- Vercel project `prj_ZoLLM35ksI6wk17PcfS2xYknaVl7`;
- Vercel team `team_1LuMlacGuM198mRjoID4O3Ct`;
- the exact store table;
- the credential version; and
- the AES key fingerprint.

A copied row, changed version, wrong encryption key, modified nonce/ciphertext/tag, or wrong project/team binding fails decryption closed.

## Compare-and-swap rotation

`loadRefreshCredential()` selects the singleton ciphertext row, verifies the key fingerprint, decrypts it, and returns exactly `{ token, version }`.

`replaceRefreshCredential({ expectedVersion, nextToken })`:

1. validates the expected version and next opaque token;
2. computes `nextVersion = expectedVersion + 1`;
3. encrypts the next token using a new random 12-byte nonce and AAD bound to `nextVersion`;
4. performs one PostgreSQL `UPDATE ... WHERE singleton AND version = expectedVersion AND key_fingerprint = ... RETURNING version`;
5. accepts success only if exactly one row is returned at exactly `nextVersion`.

Concurrent instances therefore serialize through PostgreSQL row locking and the version predicate. A stale writer receives zero rows and fails closed. The adapter has no INSERT or DELETE path.

## Database privilege contract

The live migration prepares:

- `fx558_oauth_store` - NOLOGIN, NOINHERIT, no superuser/createdb/createrole/replication/bypassrls;
- `fx558_oauth_store_login` - LOGIN, INHERIT, otherwise unprivileged, currently `PASSWORD NULL`;
- membership with `INHERIT TRUE`, `SET FALSE`, `ADMIN FALSE`;
- private schema `publication_provider_credential`;
- RLS enabled and forced on the singleton table;
- column-level SELECT on the complete ciphertext record;
- column-level UPDATE on `version`, `nonce`, `ciphertext`, `auth_tag`, `updated_at` only;
- no table-wide SELECT/UPDATE, no UPDATE of `singleton` or `key_fingerprint`;
- no INSERT, DELETE, TRUNCATE or schema CREATE;
- no direct access to `vault.secrets` or `vault.decrypted_secrets`; and
- a fail-closed assertion that neither OAuth-store role has effective EXECUTE authority on any #558 publication reader/writer function.

The existing publication functions are intentionally owned by isolated capability-owner roles (`fx558_reader_owner`, `fx558_controller_owner`, `fx558_recorder_owner`, and `fx558_revoker_owner`). The managed Supabase migration session is not permitted to act as those owners. Because the new OAuth roles have no publication-function grants, the safe migration behavior is to verify the effective boundary rather than temporarily broaden migration-session ownership merely to issue redundant revokes.

During migration preparation, the runtime verifier was corrected to inspect the **column-level** store grants with PostgreSQL `has_column_privilege()` / `has_any_column_privilege()` rather than incorrectly requiring table-level SELECT/UPDATE. This preserves the narrower database grant instead of broadening it to satisfy the verifier.

The migration intentionally created no row and no password.

## Protected provisioning sequence

The sequence must preserve this order:

1. Freshly re-lock the exact #559 head/base, CI, Preview, Production deployment, and publication/store state before each protected mutation.
2. **Complete 2026-09-15:** apply the reviewed repository migration to Production and verify the login remains passwordless, the store table is empty, RLS is forced, membership is `INHERIT TRUE / SET FALSE / ADMIN FALSE`, all intended grants are column-scoped, and all publication/Vault privileges are absent.
3. Separately approve credential provisioning before creating any password, AES key, OAuth credential, or encrypted row.
4. Register a dedicated Vercel App/OAuth client only under separate approval.
5. Install the App only on project `prj_ZoLLM35ksI6wk17PcfS2xYknaVl7` with exactly `read:project` and `read:deployment`.
6. Complete the one-time OAuth authorization-code bootstrap through an operator-controlled callback, exchange it for the initial access/refresh pair, and never persist the authorization code.
7. Generate the 32-byte AES key outside Postgres. Store it only as Production secret `PUBLICATION_GUARD_VERCEL_OAUTH_STORE_KEY`; do not put it in GitHub, issue comments, SQL literals, logs, or the database.
8. Use `preparePublicationProductionVercelOAuthStoreSeed()` outside Postgres to encrypt the initial refresh token at version `0`. Insert only version, key fingerprint, nonce, ciphertext and auth tag through an administrator connection using bound parameters.
9. Set a separate high-entropy password for `fx558_oauth_store_login`, construct the exact Shared Supavisor URL, and store it as Production-only `PUBLICATION_GUARD_VERCEL_OAUTH_STORE_DATABASE_URL`.
10. Store the Vercel App client ID/client secret through separately reviewed Production-only secret inputs. No access token is stored durably.
11. Run the store identity/privilege verification, one real refresh rotation, and the read-only provider rehearsal. Deployment-alias enumeration must succeed with exactly `read:project` + `read:deployment`; a 403 is a HOLD, not permission-expansion authority.
12. Verify the publication database still has revision `0`, zero authorizations and zero admissions before any later activation request.

Every step that mutates a provider, database, credential or Production environment remains separately protected.

## Key rotation

Rotating `PUBLICATION_GUARD_VERCEL_OAUTH_STORE_KEY` is not an ordinary refresh-token rotation. Because the key fingerprint is immutable to the runtime role, key rotation requires a separate administrator-controlled re-encryption transaction and new Production secret value. The runtime adapter intentionally cannot change `key_fingerprint`.

## Offline verification

`test-publication-production-vercel-oauth-store.mjs` continues to verify application crypto, URL/context validation, CAS behavior, error non-disclosure and fail-closed privilege classification with fake pools.

`test-publication-production-vercel-oauth-store-migration.mjs` verifies the canonical repository migration using embedded PostgreSQL only. It requires:

- executable SQL equality with the reviewed SQL contract;
- expected role attributes and membership options;
- password NULL after migration;
- forced RLS and zero seeded rows;
- column-scoped SELECT and exact CAS UPDATE columns;
- no table-wide SELECT/UPDATE and no INSERT/DELETE/TRUNCATE;
- no Vault or publication-guard authority;
- publication authority is verified by effective-privilege assertions rather than ownership-requiring function revokes;
- runtime SELECT/no-op allowed-column UPDATE behavior;
- rejection of key-fingerprint UPDATE and INSERT; and
- successful execution of the actual runtime adapter identity verifier against the migrated privilege boundary.

The migration regression does not connect to Production and creates no real provider credential.

## Still protected / held

The following remain separate protected actions:

- setting the store login password;
- creating/storing the AES key or database URL;
- registering/installing the Vercel App;
- creating/storing OAuth client credentials or refresh credentials;
- seeding the encrypted singleton row;
- importing the store/OAuth supplier into a live Production route or authority composition;
- Production redeploy/promotion;
- live provider rehearsal;
- publication authorization/admission/receipt/witness creation; and
- merge of PR #559.

Keep #558 open and #559 draft/unmerged.
