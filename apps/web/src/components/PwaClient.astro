<!--
  Service-worker registration is deliberately not global. The browser-notification
  page registers /sw.js only after the signed-in user explicitly selects Enable
  notifications and grants browser permission.
-->
